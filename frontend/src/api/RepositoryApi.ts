import { Tree } from '@blueprintjs/core';
import axios from 'axios';

export const GetRepository = (): Tree|null => {
    return null;
};

const RepositoryApi = {
    GetRepository,
}

export default RepositoryApi;